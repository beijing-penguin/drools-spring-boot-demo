package com.dc.demo.entity;
rule "2"
	no-loop true
	when
        $p : Person(age > 30);
    then
    	$p.setAge(50);
    	update($p);
		//System.out.println("hello, young xu2!");
end

query "queryPerson"
	person : Person( age > 30 );
end
